{
  "name": "orders-api-nodejs",
  "version": "1.0.0",
  "description": "Orders API with Cognito OAuth on AWS Lambda using Node.js and Express",
  "main": "src/lambda.js",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "start": "node src/app.js",
    "build": "npm run clean && npm run package",
    "clean": "rm -rf dist && mkdir -p dist",
    "package": "zip -r dist/function.zip src/ node_modules/ package.json package-lock.json -x '*.test.js' -x '__tests__/*'",
    "test": "jest",
    "lint": "eslint src/"
  },
  "dependencies": {
    "jsonwebtoken": "^9.0.2",
    "jwks-rsa": "^3.1.0",
    "@aws-sdk/client-cloudwatch": "^3.620.0"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "jest": "^29.7.0"
  },
  "keywords": [
    "aws",
    "lambda",
    "oauth",
    "cognito",
    "express",
    "api"
  ],
  "author": "",
  "license": "MIT"
}
